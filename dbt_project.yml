# dbt_project.yml
name: my_new_project
version: "1.0.0"
config-version: 2

models:
  my_new_project:
    +database: staging       # write to the 'staging' DB
    +schema: testing          # default schema for this project

    # Optional per-folder overrides
    raw:
      +schema: stage
