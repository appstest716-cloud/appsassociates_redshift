# dbt_project.yml
name: my_new_project
version: "1.0.0"
config-version: 2

models:
  my_new_project:
    +database: test      # where models are written
    +schema: testing
    raw:
      +schema: stage
