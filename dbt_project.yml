name: apps_blog
version: "1.0"
config-version: 2

models:
  apps_blog:            # MUST match folder: models/apps_blog/
    +database: staging  # Redshift database to write to
    +schema: analytics  # default schema for this package

    raw:
      +schema: stage
      materialized: table

    stage:
      +schema: post_stage
      materialized: view

    core:
      +schema: core
      materialized: table
